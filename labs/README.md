# Deprecated Paths

`collections/` is the public source of truth for lab content.

The old symlink-based `labs/` compatibility tree was removed so the repository can be mirrored cleanly through GitHub APIs. Use `bin/netlab`, `bin/stp`, and `collections/*` instead.
