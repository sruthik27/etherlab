# Rogue Root

Connects a lower-priority rogue bridge through a relay after the stable domain has converged.

Artifacts land in `local/artifacts/stp/rogue-root/`.

## Commands

- `bin/stp info rogue-root`
- `bin/stp run rogue-root`
- `bin/stp status rogue-root`
- `bin/stp down rogue-root`
